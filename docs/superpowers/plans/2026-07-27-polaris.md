# Projeto Polaris — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconstruir o Projeto Polaris como site estático de código aberto no GitHub Pages, com catálogo navegável de arquétipos de ficção científica migrado do Notion e um gerador de combinações que roda inteiramente no navegador.

**Architecture:** Astro gera HTML estático a partir de coleções de conteúdo em Markdown validadas por esquemas Zod. O acervo é uma grade de duas dimensões (tipo de recurso × subgênero) e o site abre as duas entradas. O gerador é composto por funções puras em TypeScript — sorteio e redação — testadas isoladamente e ligadas à página por um script sem framework de interface. A migração do Notion é um script Python que roda uma única vez.

**Tech Stack:** Astro 7 · TypeScript estrito · Vitest · Python 3.11+ · pytest · GitHub Actions · GitHub Pages

## Global Constraints

- **Node 22.12 ou superior**, gerenciador `npm`. Python 3.11 ou superior para os scripts de migração. O piso é 22.12 porque o Astro 7 exige essa versão, e permanecer na linha 5 do Astro significaria conviver com oito avisos de segurança sem correção disponível.
- **Sem framework de interface.** Nada de React, Vue ou Svelte. Interatividade em TypeScript puro dentro de `<script>` das páginas Astro.
- **Sem dependências de runtime no cliente.** Nenhuma biblioteca é carregada pelo navegador.
- **TypeScript em modo estrito.** `strict: true` no `tsconfig.json`.
- **Tema escuro apenas.** Não existe versão clara.
- **Acentos fixos em todo o site:** dourado `#ffc300`, violeta `#b07cff`.
- **Toda a interface em português do Brasil**, incluindo nomes de arquivos de conteúdo e mensagens de commit.
- **Licenças:** código sob MIT, conteúdo sob CC BY 4.0.
- **`prefers-reduced-motion` respeitado** por qualquer animação.
- **Testes:** Vitest para TypeScript (arquivos `*.test.ts` ao lado do código), pytest para Python (em `scripts/migracao/tests/`).
- **Nenhum teste acessa a rede.** Os testes da migração usam fixtures gravadas.
- **Commits em português, no imperativo** ("Adiciona busca global", não "Adicionada busca global").

---

## Estrutura de arquivos

```
astro.config.mjs               configuração do Astro, endereço derivado do GitHub Actions
src/
  content.config.ts            declaração das coleções, consome src/lib/schemas.ts
  lib/
    schemas.ts                 esquemas Zod — testáveis fora do Astro
    texto.ts                   slug, normalização de acentos, capitalização
    aurora.ts                  gradiente a partir do trio de cores do subgênero
    busca.ts                   índice e consulta da busca
    gerador/
      tipos.ts                 interfaces compartilhadas do gerador
      complicacoes.ts          as 40 complicações em 7 famílias
      moldes.ts                os 10 moldes de frase
      sorteio.ts               escolha das peças, travas, mistura de mundos
      redacao.ts               montagem da premissa a partir das peças
  components/                  Cartao.astro, Nav.astro, Aurora.astro, Busca.astro
  layouts/Base.astro           moldura comum, aurora, nav, rodapé
  pages/                       rotas (ver Task 7, 8, 9, 13)
  content/                     o acervo em Markdown (gerado na Task 5)
  styles/global.css            tokens de cor, tipografia, componentes de base
scripts/migracao/
  notion.py                    acesso à API do Notion (única parte que usa rede)
  extrair.py                   blocos do Notion → estrutura intermediária
  singular.py                  forma singular dos cenários
  escrever.py                  estrutura intermediária → arquivos Markdown
  executar.py                  orquestra a migração completa
  tests/                       fixtures gravadas e testes
.github/workflows/deploy.yml   build e publicação
```

---

### Task 1: Esqueleto do projeto, licenças e publicação automática

Entrega uma página vazia publicada no GitHub Pages. Colocar a publicação em pé antes de qualquer conteúdo garante que o caminho até o ar esteja provado desde o começo.

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `vitest.config.ts`
- Create: `src/pages/index.astro`, `src/layouts/Base.astro`
- Create: `LICENSE`, `LICENSE-CONTEUDO.md`, `README.md`
- Create: `.github/workflows/deploy.yml`
- Create: `src/lib/config.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `resolverBase(repositorio: string | undefined): { site: string; base: string }` exportada de `astro.config.mjs` para teste.

- [ ] **Step 1: Criar o projeto Astro**

```bash
npm create astro@latest . -- --template minimal --no-install --no-git --typescript strict --yes
npm install
npm install --save-dev vitest
```

- [ ] **Step 2: Escrever o teste da resolução de endereço**

Criar `src/lib/config.ts` vazio primeiro não é necessário — a função vive em `src/lib/config.ts` e é reexportada pelo `astro.config.mjs`.

`src/lib/config.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { resolverBase } from './config';

describe('resolverBase', () => {
  it('usa o subdiretório do repositório em um site de projeto', () => {
    expect(resolverBase('julia/polaris')).toEqual({
      site: 'https://julia.github.io',
      base: '/polaris',
    });
  });

  it('usa a raiz quando o repositório é o site do usuário', () => {
    expect(resolverBase('julia/julia.github.io')).toEqual({
      site: 'https://julia.github.io',
      base: '/',
    });
  });

  it('cai no servidor local fora do GitHub Actions', () => {
    expect(resolverBase(undefined)).toEqual({
      site: 'http://localhost:4321',
      base: '/',
    });
  });
});
```

- [ ] **Step 3: Rodar o teste e ver falhar**

Run: `npx vitest run src/lib/config.test.ts`
Expected: FAIL — `Failed to resolve import "./config"`

- [ ] **Step 4: Implementar a resolução**

`src/lib/config.ts`:

```ts
export function resolverBase(repositorio: string | undefined): { site: string; base: string } {
  if (!repositorio) {
    return { site: 'http://localhost:4321', base: '/' };
  }
  const [dono, nome] = repositorio.split('/');
  const site = `https://${dono}.github.io`;
  const base = nome === `${dono}.github.io` ? '/' : `/${nome}`;
  return { site, base };
}
```

- [ ] **Step 5: Rodar o teste e ver passar**

Run: `npx vitest run src/lib/config.test.ts`
Expected: PASS — 3 testes

- [ ] **Step 6: Ligar a resolução ao Astro**

`astro.config.mjs`:

```js
import { defineConfig } from 'astro/config';
import { resolverBase } from './src/lib/config.ts';

const { site, base } = resolverBase(process.env.GITHUB_REPOSITORY);

export default defineConfig({ site, base });
```

- [ ] **Step 7: Criar a moldura e a página inicial**

`src/layouts/Base.astro`:

```astro
---
interface Props { titulo: string }
const { titulo } = Astro.props;
---
<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{titulo} · Projeto Polaris</title>
  </head>
  <body>
    <slot />
  </body>
</html>
```

`src/pages/index.astro`:

```astro
---
import Base from '../layouts/Base.astro';
---
<Base titulo="Arquétipos da Ficção Científica">
  <h1>Projeto Polaris</h1>
</Base>
```

- [ ] **Step 8: Escrever as licenças e o README**

`LICENSE` — texto integral da licença MIT, ano 2026, titular Julia.

`LICENSE-CONTEUDO.md`:

```markdown
# Licença do conteúdo

O conteúdo editorial deste repositório — descrições de arquétipos, cenários,
elementos narrativos, textos das páginas e o banco de complicações — está sob
[Creative Commons Atribuição 4.0 Internacional (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/deed.pt-br).

Você pode usar, adaptar e redistribuir, inclusive comercialmente, creditando
Julia e o Projeto Polaris.

O que você criar a partir daqui é seu.

O código-fonte está sob licença MIT, em `LICENSE`.
```

`README.md` — o que é o projeto, como rodar (`npm install`, `npm run dev`), como contribuir com um arquétipo novo, e as duas licenças.

- [ ] **Step 9: Escrever o fluxo de publicação**

`.github/workflows/deploy.yml`:

```yaml
name: Publicar
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  construir:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx vitest run
      - uses: withastro/action@v3

  publicar:
    needs: construir
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 10: Verificar que o build passa**

Run: `npm run build && npx vitest run`
Expected: build gera `dist/index.html`; testes passam

- [ ] **Step 11: Commit**

```bash
git add -A
git commit -m "Cria esqueleto do site, licenças e publicação automática"
```

---

### Task 2: Coleções de conteúdo e validação de esquema

Os esquemas ficam em `src/lib/schemas.ts` como Zod puro, separados da declaração das coleções. Isso permite testá-los com Vitest, o que não seria possível se vivessem dentro de `astro:content`.

**Files:**
- Create: `src/lib/schemas.ts`, `src/lib/schemas.test.ts`
- Create: `src/content.config.ts`
- Create: `src/content/subgeneros/cyberpunk.md` (exemplo semeado à mão)
- Create: `src/content/arquetipos/cyberpunk/a-transumana.md` (exemplo semeado à mão)

**Interfaces:**
- Consumes: nada.
- Produces: `esquemaSubgenero`, `esquemaArquetipo`, `esquemaCenario`, `esquemaElemento`, `esquemaLivro`, `esquemaPagina` — objetos Zod exportados de `src/lib/schemas.ts`.

- [ ] **Step 1: Escrever os testes dos esquemas**

`src/lib/schemas.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { esquemaArquetipo, esquemaCenario, esquemaSubgenero } from './schemas';

describe('esquemaSubgenero', () => {
  it('exige aurora quando é um mundo', () => {
    const r = esquemaSubgenero.safeParse({ nome: 'Cyberpunk', ordem: 3, mundo: true });
    expect(r.success).toBe(false);
  });

  it('dispensa aurora quando não é um mundo', () => {
    const r = esquemaSubgenero.safeParse({
      nome: '20 Arquétipos Comuns', ordem: 7, mundo: false,
    });
    expect(r.success).toBe(true);
  });

  it('assume mundo verdadeiro por padrão', () => {
    const r = esquemaSubgenero.parse({
      nome: 'Cyberpunk', ordem: 3, aurora: ['#ff2d92', '#7c3aed', '#00e5ff'],
    });
    expect(r.mundo).toBe(true);
  });
});

describe('esquemaArquetipo', () => {
  it('aceita uma ficha completa', () => {
    const r = esquemaArquetipo.parse({ nome: 'A Transumana', subgenero: 'cyberpunk', ordem: 14 });
    expect(r.felino).toBe(false);
  });

  it('recusa ficha sem subgênero', () => {
    expect(esquemaArquetipo.safeParse({ nome: 'A Transumana', ordem: 14 }).success).toBe(false);
  });

  it('exige que o nome comece com artigo definido', () => {
    const r = esquemaArquetipo.safeParse({ nome: 'Transumana', subgenero: 'cyberpunk', ordem: 14 });
    expect(r.success).toBe(false);
  });
});

describe('esquemaCenario', () => {
  it('exige a forma singular com artigo indefinido', () => {
    const bom = esquemaCenario.safeParse({
      titulo: 'Ruínas Antigas', singular: 'uma ruína antiga', subgenero: 'space-opera', ordem: 4,
    });
    expect(bom.success).toBe(true);

    const ruim = esquemaCenario.safeParse({
      titulo: 'Ruínas Antigas', singular: 'ruína antiga', subgenero: 'space-opera', ordem: 4,
    });
    expect(ruim.success).toBe(false);
  });
});
```

O teste do artigo no nome do arquétipo não é capricho: o gerador deduz o gênero gramatical desse artigo, então uma ficha sem ele produziria frase quebrada em produção.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/schemas.test.ts`
Expected: FAIL — módulo `./schemas` não encontrado

- [ ] **Step 3: Escrever os esquemas**

`src/lib/schemas.ts`:

```ts
import { z } from 'astro/zod';

const cor = z.string().regex(/^#[0-9a-fA-F]{6}$/, 'cor deve ser hexadecimal de 6 dígitos');

export const esquemaSubgenero = z
  .object({
    nome: z.string().min(1),
    ordem: z.number().int().nonnegative(),
    mundo: z.boolean().default(true),
    aurora: z.tuple([cor, cor, cor]).optional(),
    citacao: z.string().optional(),
    citacaoAutor: z.string().optional(),
  })
  .refine((d) => !d.mundo || d.aurora !== undefined, {
    message: 'subgênero com mundo verdadeiro precisa de aurora',
    path: ['aurora'],
  });

export const esquemaArquetipo = z.object({
  nome: z.string().regex(/^[AO] ./, 'nome deve começar com o artigo "A " ou "O "'),
  subgenero: z.string().min(1),
  ordem: z.number().int().nonnegative(),
  felino: z.boolean().default(false),
});

export const esquemaCenario = z.object({
  titulo: z.string().min(1),
  singular: z.string().regex(/^(um|uma) ./, 'singular deve começar com "um " ou "uma "'),
  subgenero: z.string().min(1),
  ordem: z.number().int().nonnegative(),
});

export const esquemaElemento = z.object({
  titulo: z.string().min(1),
  subgenero: z.string().min(1),
  ordem: z.number().int().nonnegative(),
});

export const esquemaLivro = z.object({
  titulo: z.string().min(1),
  autor: z.string().min(1),
  subgenero: z.string().min(1),
  ordem: z.number().int().nonnegative(),
});

export const esquemaPagina = z.object({
  titulo: z.string().min(1),
  ordem: z.number().int().nonnegative().default(0),
});
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/schemas.test.ts`
Expected: PASS — 7 testes

- [ ] **Step 5: Declarar as coleções**

`src/content.config.ts`:

```ts
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import {
  esquemaArquetipo, esquemaCenario, esquemaElemento,
  esquemaLivro, esquemaPagina, esquemaSubgenero,
} from './lib/schemas';

// `schema: z.ZodType` puro não basta: como Output/Input são covariantes (`out`) e o
// parâmetro não recebe generics explícitos, ele é lido como `ZodType<unknown, unknown>`,
// e qualquer ZodObject concreto é atribuível a isso — inclusive o esquema errado
// (verificado empiricamente com `tsc --noEmit` passando esquemaLivro para 'arquetipos':
// nenhum erro). Por isso o parâmetro é amarrado por pasta via um tipo mapeado: assim
// o compilador rejeita um esquema estruturalmente diferente do esperado para aquela pasta.
type EsquemaPorPasta = {
  subgeneros: typeof esquemaSubgenero;
  arquetipos: typeof esquemaArquetipo;
  cenarios: typeof esquemaCenario;
  elementos: typeof esquemaElemento;
  livros: typeof esquemaLivro;
  paginas: typeof esquemaPagina;
};

const colecao = <Pasta extends keyof EsquemaPorPasta>(pasta: Pasta, schema: EsquemaPorPasta[Pasta]) =>
  defineCollection({ loader: glob({ pattern: '**/*.md', base: `./src/content/${pasta}` }), schema });

export const collections = {
  subgeneros: colecao('subgeneros', esquemaSubgenero),
  arquetipos: colecao('arquetipos', esquemaArquetipo),
  cenarios: colecao('cenarios', esquemaCenario),
  elementos: colecao('elementos', esquemaElemento),
  livros: colecao('livros', esquemaLivro),
  paginas: colecao('paginas', esquemaPagina),
};
```

- [ ] **Step 6: Semear dois arquivos de exemplo**

`src/content/subgeneros/cyberpunk.md`:

```markdown
---
nome: Cyberpunk
ordem: 3
aurora: ['#ff2d92', '#7c3aed', '#00e5ff']
citacao: A rua encontra seus próprios usos para as coisas.
citacaoAutor: William Gibson
---

União de alta tecnologia e decadência social, com megacorporações dominando,
corpos modificados por implantes e identidades fragmentadas em meio a redes digitais.
```

`src/content/arquetipos/cyberpunk/a-transumana.md`:

```markdown
---
nome: A Transumana
subgenero: cyberpunk
ordem: 14
---

Trocou tanto de si que já não sabe dizer onde termina a pessoa e começa o
projeto. Defende a própria transformação com a fé de quem não pode voltar atrás.
```

- [ ] **Step 7: Verificar que o build valida o conteúdo**

Run: `npm run build`
Expected: build passa. Em seguida, apagar temporariamente a linha `subgenero:` do arquétipo e rodar de novo — o build deve falhar apontando o campo faltante. Restaurar a linha.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "Adiciona coleções de conteúdo e validação de esquema"
```

---

### Task 3: Migração — extrair blocos do Notion

Separa o acesso à rede (`notion.py`) da interpretação dos blocos (`extrair.py`), para que a interpretação seja testável com fixtures gravadas.

**Files:**
- Create: `scripts/migracao/notion.py`, `scripts/migracao/extrair.py`
- Create: `scripts/migracao/tests/test_extrair.py`
- Create: `scripts/migracao/tests/fixtures/arquetipos_space_opera.json`
- Create: `scripts/migracao/requirements.txt`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `notion.carregar_pagina(page_id: str) -> dict` — chama a API, único ponto com rede.
  - `extrair.texto_dos_blocos(record_map: dict, raiz: str) -> list[str]` — texto dos blocos filhos, em ordem.
  - `extrair.itens(linhas: list[str]) -> list[Item]` onde `Item` é uma dataclass com `nome: str`, `descricao: str`, `felino: bool`.

- [ ] **Step 1: Gravar a fixture**

```bash
mkdir -p scripts/migracao/tests/fixtures
curl -s -X POST "https://projetopolaris.notion.site/api/v3/loadPageChunk" \
  -H "Content-Type: application/json" \
  -d '{"pageId":"2b0dfe05-91a9-8019-8fc1-eb1d998fc6bd","limit":400,"cursor":{"stack":[]},"chunkNumber":0,"verticalColumns":false}' \
  -o scripts/migracao/tests/fixtures/arquetipos_space_opera.json
```

- [ ] **Step 2: Escrever o teste da extração**

`scripts/migracao/tests/test_extrair.py`:

```python
import json
from pathlib import Path

from migracao.extrair import itens, texto_dos_blocos

FIXTURE = Path(__file__).parent / "fixtures" / "arquetipos_space_opera.json"
RAIZ = "2b0dfe05-91a9-8019-8fc1-eb1d998fc6bd"


def carregar():
    return json.loads(FIXTURE.read_text(encoding="utf-8"))["recordMap"]


def test_texto_dos_blocos_preserva_ordem():
    linhas = texto_dos_blocos(carregar(), RAIZ)
    assert any(l.startswith("O Herói Audaz:") for l in linhas)
    assert linhas.index(next(l for l in linhas if l.startswith("O Herói Audaz:"))) < linhas.index(
        next(l for l in linhas if l.startswith("A Pilota Rebelde:"))
    )


def test_itens_separa_nome_e_descricao():
    resultado = itens(texto_dos_blocos(carregar(), RAIZ))
    primeiro = resultado[0]
    assert primeiro.nome == "O Herói Audaz"
    assert primeiro.descricao.startswith("O protagonista carismático")
    assert primeiro.felino is False


def test_itens_ignora_linhas_sem_dois_pontos():
    resultado = itens(["Arquétipos por Subgênero", "O Herói Audaz: descrição"])
    assert len(resultado) == 1


def test_itens_separa_apenas_no_primeiro_dois_pontos():
    resultado = itens(["A Alienígena Diplomata/Guerreira: aliada sábia: ou adversária"])
    assert resultado[0].nome == "A Alienígena Diplomata/Guerreira"
    assert resultado[0].descricao == "aliada sábia: ou adversária"


def test_itens_marca_felino_apos_o_cabecalho():
    linhas = [
        "O Herói Audaz: descrição",
        "Arquétipo Felino",
        "O Observador espacial: um gato contemplativo da nave",
    ]
    resultado = itens(linhas)
    assert resultado[0].felino is False
    assert resultado[1].felino is True


def test_itens_encontra_vinte_e_um_arquetipos_na_space_opera():
    resultado = itens(texto_dos_blocos(carregar(), RAIZ))
    assert len(resultado) == 21
    assert sum(1 for i in resultado if i.felino) == 1
```

- [ ] **Step 3: Rodar e ver falhar**

Run: `cd scripts && python -m pytest migracao/tests/test_extrair.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'migracao.extrair'`

- [ ] **Step 4: Implementar a extração**

`scripts/migracao/extrair.py`:

```python
from dataclasses import dataclass

CABECALHO_FELINO = "arquétipo felino"


@dataclass(frozen=True)
class Item:
    nome: str
    descricao: str
    felino: bool = False


def _valor(record_map: dict, bloco_id: str) -> dict | None:
    bloco = record_map.get("block", {}).get(bloco_id)
    if not bloco:
        return None
    valor = bloco.get("value", {})
    if isinstance(valor.get("value"), dict):
        valor = valor["value"]
    return valor


def _titulo(valor: dict) -> str:
    partes = (valor.get("properties") or {}).get("title") or []
    return "".join(str(p[0]) for p in partes if isinstance(p, list) and p)


def texto_dos_blocos(record_map: dict, raiz: str) -> list[str]:
    """Texto de cada bloco filho da raiz, em ordem, sem descer em subpáginas."""
    valor = _valor(record_map, raiz)
    if not valor:
        return []
    linhas = []
    for filho_id in valor.get("content") or []:
        filho = _valor(record_map, filho_id)
        if not filho or filho.get("type") == "page":
            continue
        texto = _titulo(filho).strip()
        if texto:
            linhas.append(texto)
    return linhas


def itens(linhas: list[str]) -> list[Item]:
    """Converte linhas "Nome: descrição" em itens, marcando os que vêm após o cabeçalho felino."""
    resultado: list[Item] = []
    felino = False
    for linha in linhas:
        if linha.strip().lower() == CABECALHO_FELINO:
            felino = True
            continue
        if ":" not in linha:
            continue
        nome, descricao = linha.split(":", 1)
        nome, descricao = nome.strip(), descricao.strip()
        if not nome or not descricao:
            continue
        resultado.append(Item(nome=nome, descricao=descricao, felino=felino))
    return resultado
```

`scripts/migracao/notion.py`:

```python
import json
import urllib.request

URL = "https://projetopolaris.notion.site/api/v3/loadPageChunk"


def carregar_pagina(page_id: str) -> dict:
    """Baixa uma página do Notion. Único ponto do projeto que acessa a rede."""
    corpo = json.dumps({
        "pageId": page_id,
        "limit": 400,
        "cursor": {"stack": []},
        "chunkNumber": 0,
        "verticalColumns": False,
    }).encode()
    requisicao = urllib.request.Request(
        URL, data=corpo,
        headers={"Content-Type": "application/json", "User-Agent": "Mozilla/5.0"},
    )
    with urllib.request.urlopen(requisicao) as resposta:
        return json.load(resposta)["recordMap"]
```

`scripts/migracao/requirements.txt`:

```
pytest>=8
```

Criar também `scripts/migracao/__init__.py` e `scripts/migracao/tests/__init__.py` vazios.

- [ ] **Step 5: Rodar e ver passar**

Run: `cd scripts && python -m pytest migracao/tests/test_extrair.py -v`
Expected: PASS — 6 testes

- [ ] **Step 6: Commit**

```bash
git add scripts/
git commit -m "Adiciona extração de blocos do Notion"
```

---

### Task 4: Migração — forma singular dos cenários

A singularização automática acerta a maioria e erra em silêncio numa minoria muito visível. A estratégia é: regras cobrem o caso comum, uma tabela de exceções cobre o resto, e o resultado é revisado por pessoa antes de virar conteúdo.

**Files:**
- Create: `scripts/migracao/singular.py`
- Create: `scripts/migracao/tests/test_singular.py`

**Interfaces:**
- Consumes: nada.
- Produces: `singular.forma_singular(titulo: str) -> str` — devolve a forma com artigo indefinido, por exemplo `"uma ruína antiga"`.

- [ ] **Step 1: Escrever o teste**

`scripts/migracao/tests/test_singular.py`:

```python
import pytest

from migracao.singular import forma_singular

REGULARES = [
    ("Ruínas Antigas", "uma ruína antiga"),
    ("Estradas infinitas", "uma estrada infinita"),
    ("Megacidades superpovoadas", "uma megacidade superpovoada"),
    ("Abrigos subterrâneos", "um abrigo subterrâneo"),
    ("Mercados improvisados", "um mercado improvisado"),
    ("Comunidades fortificadas", "uma comunidade fortificada"),
    ("Terras devastadas", "uma terra devastada"),
    ("Vazios cósmicos", "um vazio cósmico"),
    ("Governos totalitários", "um governo totalitário"),
    ("Espaços abandonados", "um espaço abandonado"),
]

IRREGULARES = [
    ("Conexões Interdimensionais", "uma conexão interdimensional"),
    ("Setores industriais opressivos", "um setor industrial opressivo"),
    ("Fronteiras hostis", "uma fronteira hostil"),
    ("Habitats artificiais", "um habitat artificial"),
    ("Metrópoles Galácticas", "uma metrópole galáctica"),
    ("Corpos celestes perigosos", "um corpo celeste perigoso"),
]

EXCECOES = [
    ("Naves e frotas nômades", "uma frota nômade"),
    ("Estações e bases espaciais", "uma base espacial"),
    ("Cidades em ruínas", "uma cidade em ruínas"),
    ("Oásis de elite", "um oásis de elite"),
    ("Bairros dominados por facções", "um bairro dominado por facções"),
    ("Praças de doutrinação", "uma praça de doutrinação"),
    ("Espaços de vício e fuga", "um espaço de vício e fuga"),
]


@pytest.mark.parametrize("titulo,esperado", REGULARES + IRREGULARES + EXCECOES)
def test_forma_singular(titulo, esperado):
    assert forma_singular(titulo) == esperado
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd scripts && python -m pytest migracao/tests/test_singular.py -v`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`scripts/migracao/singular.py`:

```python
"""Forma singular dos cenários, para uso do gerador de premissas.

Regras cobrem o caso comum. A tabela EXCECOES cobre o que regra nenhuma
alcança: compostos ligados por "e" e complementos que precisam permanecer
no plural. O resultado é sempre revisado por pessoa antes de virar conteúdo.
"""

EXCECOES: dict[str, str] = {
    "Naves e frotas nômades": "uma frota nômade",
    "Estações e bases espaciais": "uma base espacial",
    "Cidades em ruínas": "uma cidade em ruínas",
    "Oásis de elite": "um oásis de elite",
    "Bairros dominados por facções": "um bairro dominado por facções",
}

# Palavras que revelam o início de um complemento: tudo dali em diante
# fica intocado, porque singularizá-lo produziria frase errada.
PREPOSICOES = {"de", "do", "da", "dos", "das", "em", "por", "para", "com", "sem", "a", "e"}

FEMININAS = ("a", "ã", "ade", "agem", "ção", "são", "ise", "ie")


def _singularizar_palavra(palavra: str) -> str:
    # A guarda dos invariáveis vem primeiro de propósito: "oásis" termina em
    # "is" e seria mutilado em "oásil" pela regra de -is → -il se ela viesse antes.
    if palavra.endswith("sis") or palavra.endswith("pis"):
        return palavra
    if palavra.endswith("ões"):
        return palavra[:-3] + "ão"
    if palavra.endswith("ães"):
        return palavra[:-3] + "ão"
    if palavra.endswith("ais"):
        return palavra[:-3] + "al"
    if palavra.endswith("éis"):
        return palavra[:-3] + "el"
    if palavra.endswith("eis"):
        return palavra[:-3] + "el"
    if palavra.endswith("óis"):
        return palavra[:-3] + "ol"
    if palavra.endswith("is") and len(palavra) > 3:
        return palavra[:-2] + "il"
    if palavra.endswith("ns"):
        return palavra[:-2] + "m"
    if palavra.endswith("res") or palavra.endswith("ses") or palavra.endswith("zes"):
        return palavra[:-2]
    if palavra.endswith("s") and not palavra.endswith("ss"):
        # "oásis", "lápis" e afins são invariáveis
        if palavra.endswith("sis") or palavra.endswith("pis"):
            return palavra
        return palavra[:-1]
    return palavra


def _e_feminina(palavra: str) -> bool:
    return palavra.endswith(FEMININAS)


def forma_singular(titulo: str) -> str:
    if titulo in EXCECOES:
        return EXCECOES[titulo]

    palavras = titulo.split()
    saida: list[str] = []
    parou = False
    for palavra in palavras:
        if palavra.lower() in PREPOSICOES:
            parou = True
        saida.append(palavra if parou else _singularizar_palavra(palavra.lower()))

    nucleo = saida[0]
    artigo = "uma" if _e_feminina(nucleo) else "um"
    return f"{artigo} {' '.join(saida)}"
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd scripts && python -m pytest migracao/tests/test_singular.py -v`
Expected: PASS — 23 casos

Se algum caso irregular resistir, acrescentá-lo a `EXCECOES` é resposta legítima: a tabela existe exatamente para isso, e conteúdo correto vale mais que regra elegante.

- [ ] **Step 5: Commit**

```bash
git add scripts/
git commit -m "Adiciona forma singular dos cenários"
```

---

### Task 5: Migração — escrever os arquivos e rodar

**Files:**
- Create: `scripts/migracao/escrever.py`, `scripts/migracao/executar.py`
- Create: `scripts/migracao/tests/test_escrever.py`
- Create: `src/content/**` (gerado)

**Interfaces:**
- Consumes: `extrair.Item`, `singular.forma_singular`.
- Produces:
  - `escrever.slug(texto: str) -> str`
  - `escrever.arquivo_markdown(frontmatter: dict, corpo: str) -> str`
  - `escrever.escrever_arquetipos(itens: list[Item], subgenero: str, destino: Path) -> int`

- [ ] **Step 1: Escrever o teste**

`scripts/migracao/tests/test_escrever.py`:

```python
from migracao.escrever import arquivo_markdown, escrever_arquetipos, slug
from migracao.extrair import Item


def test_slug_remove_acentos_e_artigos_ficam():
    assert slug("A Transumana") == "a-transumana"
    assert slug("O Fixer/Corretor") == "o-fixer-corretor"
    assert slug("Pós Apocalíptico") == "pos-apocaliptico"
    assert slug("Naves e frotas nômades") == "naves-e-frotas-nomades"


def test_arquivo_markdown_monta_frontmatter_e_corpo():
    texto = arquivo_markdown({"nome": "A Transumana", "ordem": 14}, "Trocou tanto de si.")
    assert texto.startswith("---\n")
    assert 'nome: "A Transumana"' in texto
    assert "ordem: 14" in texto
    assert texto.endswith("Trocou tanto de si.\n")


def test_arquivo_markdown_escapa_aspas():
    texto = arquivo_markdown({"nome": 'O "Fantasma"'}, "corpo")
    assert 'nome: "O \\"Fantasma\\""' in texto


def test_escrever_arquetipos_cria_um_arquivo_por_item(tmp_path):
    itens = [
        Item("O Herói Audaz", "O protagonista carismático."),
        Item("O Observador espacial", "Um gato contemplativo.", felino=True),
    ]
    total = escrever_arquetipos(itens, "space-opera", tmp_path)

    assert total == 2
    criado = (tmp_path / "space-opera" / "o-heroi-audaz.md").read_text(encoding="utf-8")
    assert "subgenero: space-opera" in criado
    assert "ordem: 1" in criado
    assert "felino: false" in criado

    gato = (tmp_path / "space-opera" / "o-observador-espacial.md").read_text(encoding="utf-8")
    assert "felino: true" in gato
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `cd scripts && python -m pytest migracao/tests/test_escrever.py -v`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`scripts/migracao/escrever.py`:

```python
import re
import unicodedata
from pathlib import Path

from .extrair import Item


def slug(texto: str) -> str:
    sem_acento = unicodedata.normalize("NFKD", texto).encode("ascii", "ignore").decode()
    return re.sub(r"-+", "-", re.sub(r"[^a-z0-9]+", "-", sem_acento.lower())).strip("-")


def _valor_yaml(valor) -> str:
    if isinstance(valor, bool):
        return "true" if valor else "false"
    if isinstance(valor, (int, float)):
        return str(valor)
    if isinstance(valor, list):
        return "[" + ", ".join(_valor_yaml(v) for v in valor) + "]"
    return '"' + str(valor).replace('\\', '\\\\').replace('"', '\\"') + '"'


def arquivo_markdown(frontmatter: dict, corpo: str) -> str:
    linhas = [f"{chave}: {_valor_yaml(valor)}" for chave, valor in frontmatter.items()]
    return "---\n" + "\n".join(linhas) + "\n---\n\n" + corpo.strip() + "\n"


def escrever_arquetipos(itens: list[Item], subgenero: str, destino: Path) -> int:
    pasta = destino / subgenero
    pasta.mkdir(parents=True, exist_ok=True)
    for ordem, item in enumerate(itens, start=1):
        conteudo = arquivo_markdown(
            {"nome": item.nome, "subgenero": subgenero, "ordem": ordem, "felino": item.felino},
            item.descricao,
        )
        (pasta / f"{slug(item.nome)}.md").write_text(conteudo, encoding="utf-8")
    return len(itens)
```

- [ ] **Step 4: Rodar e ver passar**

Run: `cd scripts && python -m pytest migracao/tests/ -v`
Expected: PASS — todos os testes das três tarefas de migração

- [ ] **Step 5: Escrever o orquestrador**

`scripts/migracao/executar.py` — mapeia os identificadores de página do Notion para subgêneros e coleções, chama `notion.carregar_pagina`, `extrair.itens` e as funções de escrita, e imprime um resumo com a contagem por coleção. Os identificadores conhecidos:

```python
SUBGENEROS = {
    "space-opera": {"nome": "Space Opera", "ordem": 1,
                    "arquetipos": "2b0dfe05-91a9-8019-8fc1-eb1d998fc6bd",
                    "elementos": "2b1dfe05-91a9-8078-aac5-dab46ae350e0",
                    "cenarios": "2c5dfe05-91a9-803f-bfbf-e465c02b3ffc"},
    "distopia": {"nome": "Distopia", "ordem": 2,
                 "arquetipos": "2b0dfe05-91a9-80ef-ba87-e04f9581e3d0",
                 "elementos": "2b1dfe05-91a9-8027-9639-ce151dc7bc92",
                 "cenarios": "2c5dfe05-91a9-800f-b0e7-e11ca013d34b"},
    "cyberpunk": {"nome": "Cyberpunk", "ordem": 3,
                  "arquetipos": "2b0dfe05-91a9-807b-89f0-e6e3bc9b7e62",
                  "elementos": "2b1dfe05-91a9-80de-af26-fd0224f62efe",
                  "cenarios": "2c5dfe05-91a9-80f9-bbc5-c64a177cad0e"},
    "pos-apocaliptico": {"nome": "Pós Apocalíptico", "ordem": 4,
                         "arquetipos": "2b0dfe05-91a9-8016-a1b4-c5aa0adcc5a1",
                         "elementos": "2b1dfe05-91a9-80f0-aec4-c8dd59c2fd78",
                         "cenarios": "2c5dfe05-91a9-8049-82eb-c8e7a790afea"},
    "invasao-alienigena": {"nome": "Invasão Alienígena", "ordem": 5,
                           "arquetipos": "2b0dfe05-91a9-8033-8404-fc54bf06b6e7",
                           "elementos": "2b1dfe05-91a9-80c5-b3fa-d71da2f11906",
                           "cenarios": "2c5dfe05-91a9-80f8-9be0-c3c7e85155e9"},
    "viagem-no-tempo": {"nome": "Viagem no Tempo", "ordem": 6,
                        "arquetipos": "2b0dfe05-91a9-80a8-86d9-f6bc78362c2c",
                        "elementos": "2b1dfe05-91a9-807c-9410-c0802ef55499",
                        "cenarios": "2c5dfe05-91a9-8080-871a-f553d2afa05f"},
    "comuns": {"nome": "20 Arquétipos Comuns", "ordem": 7, "mundo": False,
               "arquetipos": "2b1dfe05-91a9-806d-a72c-f959b1d2a4af"},
}
LIVROS = "2b1dfe05-91a9-80d5-86bf-dba736b11deb"
```

As páginas de cenários são blocos do tipo `toggle`, e não subpáginas: `notion.carregar_pagina` aceita o identificador do toggle da mesma forma.

- [ ] **Step 6: Rodar a migração de verdade**

Run: `cd scripts && python -m migracao.executar --destino ../src/content`
Expected: resumo com ~146 arquétipos, 60 cenários, ~120 elementos, 36 livros

- [ ] **Step 7: Escolher as auroras e completar os subgêneros**

Preencher `aurora` em cada um dos seis arquivos de subgênero. Ponto de partida validado no design:

| Subgênero | Aurora |
|---|---|
| Cyberpunk | `['#ff2d92', '#7c3aed', '#00e5ff']` |
| Space Opera | `['#ffc300', '#ff7a45', '#b07cff']` |
| Distopia | `['#ff5f45', '#7a1f2b', '#ffb03a']` |
| Pós-Apocalíptico | `['#6ee7a0', '#2f6b4f', '#c8b47a']` |
| Invasão Alienígena | `['#35e5f0', '#1c5e8f', '#a6ff6e']` |
| Viagem no Tempo | `['#b07cff', '#3b2f8f', '#ffd66e']` |

- [ ] **Step 8: Revisar o acervo**

Conferir à mão: as formas `singular` dos 60 cenários, o texto do "Sobre" (copiado à mão, não migrado), e uma amostra de dez arquétipos por subgênero. Corrigir o que o script tiver entendido torto.

- [ ] **Step 9: Verificar que tudo valida**

Run: `npm run build`
Expected: build passa com o acervo completo

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "Migra o acervo do Notion para o repositório"
```

---

### Task 6: Sistema visual — tokens, aurora e moldura

**Files:**
- Create: `src/styles/global.css`
- Create: `src/lib/aurora.ts`, `src/lib/aurora.test.ts`
- Create: `src/components/Aurora.astro`, `src/components/Nav.astro`
- Modify: `src/layouts/Base.astro`

**Interfaces:**
- Consumes: `esquemaSubgenero` (campo `aurora`).
- Produces: `aurora.gradienteConico(cores: readonly [string, string, string] | undefined): string` — devolve o valor CSS do fundo, com a aurora padrão quando não há trio.

- [ ] **Step 1: Escrever o teste da aurora**

`src/lib/aurora.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { AURORA_PADRAO, gradienteConico } from './aurora';

describe('gradienteConico', () => {
  it('fecha o círculo repetindo a primeira cor', () => {
    const css = gradienteConico(['#ff2d92', '#7c3aed', '#00e5ff']);
    expect(css).toBe('conic-gradient(from 200deg, #ff2d92, #7c3aed, #00e5ff, #ff2d92)');
  });

  it('cai na aurora padrão quando o subgênero não é um mundo', () => {
    expect(gradienteConico(undefined)).toBe(gradienteConico(AURORA_PADRAO));
  });
});
```

Repetir a primeira cor no fim importa: sem isso o gradiente cônico mostra uma emenda dura entre a última e a primeira cor.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/aurora.test.ts`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`src/lib/aurora.ts`:

```ts
export const AURORA_PADRAO = ['#ff2d92', '#7c3aed', '#00e5ff'] as const;

export function gradienteConico(cores: readonly [string, string, string] | undefined): string {
  const [a, b, c] = cores ?? AURORA_PADRAO;
  return `conic-gradient(from 200deg, ${a}, ${b}, ${c}, ${a})`;
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/aurora.test.ts`
Expected: PASS — 2 testes

- [ ] **Step 5: Escrever os tokens e o estilo base**

`src/styles/global.css` — tokens e componentes de base. Os acentos são fixos; só a aurora varia:

```css
:root {
  --ouro: #ffc300;
  --violeta: #b07cff;
  --tinta: #0b0b0e;
  --papel: #ffffff;
  --texto: #d5d5df;
  --apagado: #8e8e9c;
  --painel: rgba(11, 11, 14, 0.8);
  --borda-suave: rgba(255, 255, 255, 0.16);
}

* { box-sizing: border-box; }

body {
  margin: 0;
  background: var(--tinta);
  color: var(--texto);
  font-family: 'Segoe UI', system-ui, sans-serif;
  min-height: 100vh;
}

h1, h2, h3 {
  color: var(--papel);
  font-weight: 900;
  letter-spacing: -0.04em;
  text-transform: uppercase;
  line-height: 0.95;
}

.cartao {
  background: var(--painel);
  backdrop-filter: blur(9px);
  border: 2px solid var(--ouro);
  padding: 16px;
}

.cartao p {
  border-left: 3px solid var(--violeta);
  padding-left: 10px;
  margin: 0;
  line-height: 1.55;
}

.etiqueta {
  display: inline-block;
  background: var(--ouro);
  color: var(--tinta);
  font-weight: 900;
  font-size: 0.7rem;
  letter-spacing: 0.16em;
  padding: 4px 8px;
  text-transform: uppercase;
}

.botao {
  border: 2px solid var(--violeta);
  color: var(--violeta);
  background: transparent;
  font-weight: 800;
  text-transform: uppercase;
  padding: 8px 14px;
  cursor: pointer;
  box-shadow: 3px 3px 0 color-mix(in srgb, var(--violeta) 28%, transparent);
}

.botao--principal {
  background: var(--ouro);
  border-color: var(--ouro);
  color: var(--tinta);
  box-shadow: 3px 3px 0 color-mix(in srgb, var(--ouro) 30%, transparent);
}

.botao:focus-visible, a:focus-visible {
  outline: 3px solid var(--violeta);
  outline-offset: 3px;
}
```

- [ ] **Step 6: Escrever o componente da aurora**

`src/components/Aurora.astro`:

```astro
---
import { gradienteConico } from '../lib/aurora';
interface Props { cores?: readonly [string, string, string] }
const { cores } = Astro.props;
---
<div class="aurora" style={`--gradiente: ${gradienteConico(cores)}`} aria-hidden="true"></div>

<style>
  .aurora {
    position: fixed;
    inset: -45%;
    z-index: -1;
    background: var(--gradiente);
    filter: blur(58px);
    opacity: 0.42;
    animation: girar 90s linear infinite;
  }

  @keyframes girar {
    to { transform: rotate(360deg); }
  }

  @media (prefers-reduced-motion: reduce) {
    .aurora { animation: none; }
  }
</style>
```

- [ ] **Step 7: Montar a navegação e ligar tudo na moldura**

`src/components/Nav.astro` com marca "POLARIS" e links para Arquétipos, Cenários, Elementos, Estilos, Livros, Gerador e Sobre — todos prefixados por `import.meta.env.BASE_URL`, sem o que os links quebram no GitHub Pages. `Base.astro` importa `global.css`, recebe uma propriedade opcional `aurora` e monta `<Aurora>`, `<Nav>` e o rodapé com as duas licenças.

- [ ] **Step 8: Ver no navegador**

Run: `npm run dev`
Expected: fundo com aurora girando lentamente; com "reduzir movimento" ativado no sistema, parada. Conferir que a página não rola horizontalmente em uma janela de 360px de largura.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "Adiciona sistema visual, aurora e navegação"
```

---

### Task 7: Páginas de catálogo por tipo e subgênero

**Files:**
- Create: `src/components/Cartao.astro`
- Create: `src/pages/arquetipos/index.astro`, `src/pages/arquetipos/[subgenero].astro`
- Create: `src/pages/cenarios/index.astro`, `src/pages/cenarios/[subgenero].astro`
- Create: `src/pages/elementos/index.astro`, `src/pages/elementos/[subgenero].astro`
- Create: `src/pages/livros/index.astro`, `src/pages/livros/[subgenero].astro`
- Create: `src/pages/sobre.astro`, `src/pages/estilos.astro`
- Create: `src/lib/texto.ts`, `src/lib/texto.test.ts`

**Interfaces:**
- Consumes: coleções da Task 2, `Aurora` e `Nav` da Task 6.
- Produces: `texto.paraAncora(nome: string): string` — usado tanto pelas páginas quanto pelos links diretos.

- [ ] **Step 1: Escrever o teste da âncora**

`src/lib/texto.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { paraAncora } from './texto';

describe('paraAncora', () => {
  it('remove acentos e junta com hífen', () => {
    expect(paraAncora('A Transumana')).toBe('a-transumana');
    expect(paraAncora('O Fixer/Corretor')).toBe('o-fixer-corretor');
    expect(paraAncora('Pós Apocalíptico')).toBe('pos-apocaliptico');
  });

  it('não deixa hífen sobrando nas pontas', () => {
    expect(paraAncora('  O Hacker  ')).toBe('o-hacker');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/texto.test.ts`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`src/lib/texto.ts`:

```ts
export function semAcento(texto: string): string {
  // NFKD separa a letra do seu acento; a propriedade Unicode Diacritic remove os
  // acentos soltos. Usar a propriedade, e não uma faixa de caracteres literais,
  // evita que um editor recomponha os acentos e quebre a expressão em silêncio.
  return texto.normalize('NFKD').replace(/\p{Diacritic}/gu, '');
}

export function paraAncora(nome: string): string {
  return semAcento(nome)
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '');
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/texto.test.ts`
Expected: PASS — 2 testes

- [ ] **Step 5: Escrever o cartão**

`src/components/Cartao.astro` recebe `etiqueta`, `titulo`, `ancora` e o corpo por slot. Renderiza com as classes `.cartao` e `.etiqueta` e coloca `id={ancora}` no elemento raiz, que é o que torna o link direto possível.

- [ ] **Step 6: Escrever as páginas de índice e de subgênero**

Cada índice lista os subgêneros com `getCollection('subgeneros')` ordenados por `ordem`. Cada página de subgênero usa `getStaticPaths` sobre os subgêneros e filtra a coleção correspondente:

```astro
---
import { getCollection } from 'astro:content';
import Base from '../../layouts/Base.astro';
import Cartao from '../../components/Cartao.astro';
import { paraAncora } from '../../lib/texto';

export async function getStaticPaths() {
  const subgeneros = await getCollection('subgeneros');
  return subgeneros.map((s) => ({ params: { subgenero: s.id }, props: { subgenero: s } }));
}

const { subgenero } = Astro.props;
const fichas = (await getCollection('arquetipos', (a) => a.data.subgenero === subgenero.id))
  .sort((a, b) => a.data.ordem - b.data.ordem);
const comuns = fichas.filter((f) => !f.data.felino);
const felino = fichas.find((f) => f.data.felino);
---
<Base titulo={`Arquétipos ${subgenero.data.nome}`} aurora={subgenero.data.aurora}>
  <h1>Arquétipos {subgenero.data.nome}</h1>
  {comuns.map((f) => (
    <Cartao etiqueta={subgenero.data.nome} titulo={f.data.nome} ancora={paraAncora(f.data.nome)}>
      <p>{f.body}</p>
    </Cartao>
  ))}
  {felino && (
    <section aria-label="Arquétipo felino">
      <Cartao etiqueta="Arquétipo Felino" titulo={felino.data.nome} ancora={paraAncora(felino.data.nome)}>
        <p>{felino.body}</p>
      </Cartao>
    </section>
  )}
  {subgenero.data.citacao && (
    <blockquote>{subgenero.data.citacao}<cite>{subgenero.data.citacaoAutor}</cite></blockquote>
  )}
</Base>
```

As páginas de cenários, elementos e livros seguem a mesma forma, trocando a coleção e o campo de título — `titulo` em vez de `nome` — e omitindo o bloco do felino, que só existe em arquétipos. A página de livros mostra também `autor`. A citação aparece em todas as quatro, pois vem do subgênero e não da coleção.

O bloco **"Como usar esta página"** é conteúdo de seção, não de subgênero — a migração da Task 5 confirmou que no Notion ele existe uma vez por seção, não um por mundo. Ele vive em `src/content/paginas/{arquetipos,cenarios,elementos,livros}.md`, junto do texto de abertura daquela seção, e é renderizado na página índice da seção.

Cada subgênero tem dois textos próprios, com funções distintas: o campo `aberturaArquetipos` abre a página `/arquetipos/[subgenero]/`, e o corpo do arquivo de subgênero descreve o mundo, abrindo `/elementos/[subgenero]/` e a página do mundo.

**Renderização de Markdown:** as descrições de arquétipos, cenários, elementos e livros são prosa simples de um parágrafo e podem ser exibidas como texto. Já os arquivos de `paginas/` contêm cabeçalhos, listas e ao menos um link (o e-mail no "Sobre"), então precisam ser renderizados de verdade com `render()` de `astro:content` — exibi-los como texto cru mostraria a sintaxe Markdown na tela.

`sobre.astro` e `estilos.astro` renderizam as entradas da coleção `paginas`.

- [ ] **Step 7: Verificar as rotas geradas**

Run: `npm run build`
Expected: `dist/arquetipos/cyberpunk/index.html` existe e contém `id="a-transumana"`; existem 7 páginas de arquétipos, 6 de cenários, 6 de elementos e 6 de livros

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "Adiciona páginas de catálogo por tipo e subgênero"
```

---

### Task 8: Página do mundo

Cruza o outro eixo da grade: tudo de um subgênero reunido numa página.

**Files:**
- Create: `src/pages/mundos/[subgenero].astro`
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Escrever a página**

`getStaticPaths` filtra apenas os subgêneros com `mundo: true` — o pool "20 Arquétipos Comuns" não é um mundo e não ganha rota. A página reúne, em seções com sumário no topo, os arquétipos, cenários, elementos e livros daquele subgênero, cada seção com link para a página completa correspondente.

```astro
export async function getStaticPaths() {
  const subgeneros = await getCollection('subgeneros', (s) => s.data.mundo);
  return subgeneros.map((s) => ({ params: { subgenero: s.id }, props: { subgenero: s } }));
}
```

- [ ] **Step 2: Escrever a home**

`index.astro` apresenta o projeto, os três pilares com link, os seis mundos e uma chamada para o gerador.

- [ ] **Step 3: Verificar**

Run: `npm run build`
Expected: existem exatamente 6 páginas em `dist/mundos/`; não existe `dist/mundos/comuns/`

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "Adiciona página do mundo e a home"
```

---

### Task 9: Busca global

**Files:**
- Create: `src/lib/busca.ts`, `src/lib/busca.test.ts`
- Create: `src/pages/indice-busca.json.ts`
- Create: `src/components/Busca.astro`
- Modify: `src/components/Nav.astro`

**Interfaces:**
- Consumes: `texto.semAcento`.
- Produces:
  - `type ItemIndice = { titulo: string; tipo: string; subgenero: string; url: string }`
  - `buscar(indice: ItemIndice[], consulta: string): ItemIndice[]`

- [ ] **Step 1: Escrever o teste**

`src/lib/busca.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { buscar, type ItemIndice } from './busca';

const indice: ItemIndice[] = [
  { titulo: 'A Transumana', tipo: 'arquetipo', subgenero: 'cyberpunk', url: '/a' },
  { titulo: 'Ruínas Antigas', tipo: 'cenario', subgenero: 'space-opera', url: '/b' },
  { titulo: 'Vigilância onipresente', tipo: 'elemento', subgenero: 'distopia', url: '/c' },
];

describe('buscar', () => {
  it('ignora acentuação na consulta e no acervo', () => {
    expect(buscar(indice, 'ruinas')[0].titulo).toBe('Ruínas Antigas');
    expect(buscar(indice, 'vigilância')[0].titulo).toBe('Vigilância onipresente');
  });

  it('ignora caixa', () => {
    expect(buscar(indice, 'TRANSUMANA')).toHaveLength(1);
  });

  it('devolve vazio para consulta em branco', () => {
    expect(buscar(indice, '   ')).toEqual([]);
  });

  it('prioriza quem começa com a consulta', () => {
    const comAmbos: ItemIndice[] = [
      { titulo: 'Uma cidade em ruínas', tipo: 'cenario', subgenero: 'x', url: '/1' },
      { titulo: 'Ruínas tecnológicas', tipo: 'cenario', subgenero: 'x', url: '/2' },
    ];
    expect(buscar(comAmbos, 'ruinas')[0].url).toBe('/2');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/busca.test.ts`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`src/lib/busca.ts`:

```ts
import { semAcento } from './texto';

export type ItemIndice = { titulo: string; tipo: string; subgenero: string; url: string };

function normalizar(texto: string): string {
  return semAcento(texto).toLowerCase().trim();
}

export function buscar(indice: ItemIndice[], consulta: string): ItemIndice[] {
  const alvo = normalizar(consulta);
  if (!alvo) return [];

  return indice
    .map((item) => ({ item, posicao: normalizar(item.titulo).indexOf(alvo) }))
    .filter(({ posicao }) => posicao >= 0)
    .sort((a, b) => a.posicao - b.posicao || a.item.titulo.localeCompare(b.item.titulo, 'pt-BR'))
    .map(({ item }) => item);
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/busca.test.ts`
Expected: PASS — 4 testes

- [ ] **Step 5: Gerar o índice no build**

`src/pages/indice-busca.json.ts` percorre as quatro coleções e devolve um `Response` com o JSON de `ItemIndice[]`, incluindo a âncora na URL de cada item.

- [ ] **Step 6: Ligar a interface**

`src/components/Busca.astro` traz um `<input type="search">` e um `<script>` que baixa o índice na primeira digitação, chama `buscar` e mostra até 10 resultados. Navegação por seta para cima e para baixo, Enter para abrir, Escape para fechar.

- [ ] **Step 7: Verificar**

Run: `npm run build`
Expected: `dist/indice-busca.json` existe e contém mais de 300 itens. No navegador, digitar "cenario" traz resultados com acento.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "Adiciona busca global"
```

---

### Task 10: Dados do gerador — complicações e moldes

**Files:**
- Create: `src/lib/gerador/tipos.ts`, `src/lib/gerador/complicacoes.ts`, `src/lib/gerador/moldes.ts`
- Create: `src/lib/gerador/dados.test.ts`

**Interfaces:**
- Produces:
  - `type Familia = { nome: string; complicacoes: string[] }`
  - `COMPLICACOES: Familia[]` — 7 famílias, 40 complicações
  - `MOLDES: string[]` — 10 moldes com marcadores

- [ ] **Step 1: Escrever o teste dos dados**

`src/lib/gerador/dados.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { COMPLICACOES } from './complicacoes';
import { MOLDES } from './moldes';

describe('COMPLICACOES', () => {
  it('tem sete famílias e quarenta complicações', () => {
    expect(COMPLICACOES).toHaveLength(7);
    expect(COMPLICACOES.flatMap((f) => f.complicacoes)).toHaveLength(40);
  });

  it('não tem complicação repetida', () => {
    const todas = COMPLICACOES.flatMap((f) => f.complicacoes);
    expect(new Set(todas).size).toBe(40);
  });

  it('começa em minúscula e não termina em ponto, para encaixar no molde', () => {
    for (const texto of COMPLICACOES.flatMap((f) => f.complicacoes)) {
      expect(texto[0]).toBe(texto[0].toLowerCase());
      expect(texto.endsWith('.')).toBe(false);
    }
  });

  it('nenhuma família fica vazia', () => {
    for (const familia of COMPLICACOES) expect(familia.complicacoes.length).toBeGreaterThan(0);
  });
});

describe('MOLDES', () => {
  it('tem dez moldes', () => {
    expect(MOLDES).toHaveLength(10);
  });

  it('todo molde usa as quatro peças', () => {
    for (const molde of MOLDES) {
      expect(molde).toMatch(/\{(em:|a:)?cenario\}/);
      expect(molde).toContain('{elemento}');
      expect(molde).toContain('{arquetipo}');
      expect(molde).toContain('{complicacao}');
    }
  });

  it('todo molde termina em ponto final', () => {
    for (const molde of MOLDES) expect(molde.endsWith('.')).toBe(true);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/gerador/dados.test.ts`
Expected: FAIL — módulos não encontrados

- [ ] **Step 3: Escrever as complicações**

`src/lib/gerador/complicacoes.ts` — as 40 do Apêndice A do documento de design, agrupadas nas sete famílias: "Traição e confiança" (6), "O custo moral" (6), "Identidade e revelação" (6), "Tempo e urgência" (6), "O preço do poder" (6), "O inimigo que não é inimigo" (5), "Perda e memória" (5).

```ts
import type { Familia } from './tipos';

export const COMPLICACOES: Familia[] = [
  {
    nome: 'Traição e confiança',
    complicacoes: [
      'quem pagou pelo serviço já sabia a resposta que mandou buscar',
      'o aviso que evitou a morte veio de quem mais lucraria com ela',
      'a única pessoa digna de confiança ali cumpre ordens desde o primeiro dia',
      'a escolha não foi por competência, e sim por ser fácil de descartar depois',
      'alguém do mesmo lado já negociou a rendição de todos, e negociou bem',
      'o segredo foi guardado a vida inteira por quem prometeu revelá-lo',
    ],
  },
  {
    nome: 'O custo moral',
    complicacoes: [
      'salvar a maioria exige entregar exatamente quem confiou primeiro',
      'a solução funciona, e funciona porque alguém aceitou não voltar',
      'o certo e o possível apontam para lados opostos, e só há tempo para um',
      'fazer a coisa certa agora condena quem vier depois',
      'o silêncio custa uma vida por dia, e falar custa todas de uma vez',
      'existe uma saída limpa, e ela serve para uma pessoa só',
    ],
  },
  {
    nome: 'Identidade e revelação',
    complicacoes: [
      'a lembrança mais antiga que possui foi plantada, e plantada com capricho',
      'o inimigo procurado há anos usa o próprio rosto',
      'pertence, por nascimento, ao lado que jurou destruir',
      'o nome que carrega pertenceu antes a outra pessoa, e essa pessoa não morreu',
      'existe para isso desde antes de nascer, e nunca houve escolha nenhuma',
      'a assinatura no documento que condenou todo mundo é a sua',
    ],
  },
  {
    nome: 'Tempo e urgência',
    complicacoes: [
      'o prazo que parecia longo já venceu em algum lugar',
      'cada tentativa de consertar aproxima o desastre em um dia',
      'a decisão precisa ser tomada antes de existir informação suficiente',
      'o resgate está a caminho e chega depois do que acontecer primeiro',
      'há uma janela, e ela se fecha com alguém ainda do lado de dentro',
      'o aviso chegou no tempo certo, mas para a pessoa errada',
    ],
  },
  {
    nome: 'O preço do poder',
    complicacoes: [
      'a única ferramenta capaz de vencer cobra um pedaço de quem a usa',
      'aceitar a ajuda oferecida significa dever um favor impagável',
      'usar a arma uma vez basta para nunca mais conseguir largá-la',
      'a cura existe, e é fabricada com a própria doença',
      'o comando só obedece a quem já perdeu o que amava',
      'quem detém o poder o mantém justamente por não usá-lo',
    ],
  },
  {
    nome: 'O inimigo que não é inimigo',
    complicacoes: [
      'o outro lado tem razão, e provar isso não muda o que precisa ser feito',
      'o monstro está fazendo exatamente o que qualquer um faria no lugar dele',
      'a ordem absurda recebida era a única capaz de evitar algo pior',
      'quem deveria ser resgatado não quer sair de onde está',
      'a vitória já aconteceu, e ninguém percebeu porque não se parece com vitória',
    ],
  },
  {
    nome: 'Perda e memória',
    complicacoes: [
      'lembrar a verdade exige perder tudo o que veio depois dela',
      'a prova procurada existe apenas na cabeça de quem já esqueceu',
      'voltar continua sendo possível, mas não há mais para onde',
      'todo mundo mudou de lugar, menos quem ficou esperando',
      'a casa continua lá, intacta, e é isso que torna impossível voltar',
    ],
  },
];
```

- [ ] **Step 4: Escrever os moldes**

`src/lib/gerador/moldes.ts` — dez moldes. Nenhum usa adjetivo ou particípio referente ao arquétipo, o que evita erro de concordância; os dois que precisam de pronome usam o marcador `{pronome}`, resolvido pelo gênero deduzido do artigo do nome.

```ts
export const MOLDES: string[] = [
  '{em:cenario}, sob {elemento}, {arquetipo} descobre que {complicacao}.',
  '{em:cenario}, onde impera {elemento}, {arquetipo} precisa aceitar que {complicacao}.',
  '{em:cenario}, {elemento} é a regra — e {arquetipo} descobre que {complicacao}.',
  'tudo começa {em:cenario}, num mundo de {elemento}: {arquetipo} descobre que {complicacao}.',
  '{arquetipo} sobrevive {em:cenario}, onde impera {elemento}, até {pronome} descobrir que {complicacao}.',
  '{em:cenario}, com {elemento} por todo lado, {arquetipo} entende tarde demais que {complicacao}.',
  'ninguém avisou {arquetipo} de que, {em:cenario} sob {elemento}, {complicacao}.',
  '{em:cenario}, {arquetipo} enfrenta {elemento} e precisa aceitar que {complicacao}.',
  '{arquetipo} chega {a:cenario} e encontra {elemento}. O que ninguém contou: {complicacao}.',
  'há {elemento} {em:cenario}, e {arquetipo} descobre tarde demais que {complicacao}.',
];
```

`src/lib/gerador/tipos.ts`:

```ts
export type Familia = { nome: string; complicacoes: string[] };

export type Peca = { id: string; nome: string; subgenero: string };
export type PecaCenario = Peca & { singular: string };

export type Sorteio = {
  arquetipo: Peca;
  cenario: PecaCenario;
  elemento: Peca;
  complicacao: string;
  familia: string;
};

export type Pools = {
  arquetipos: Peca[];
  cenarios: PecaCenario[];
  elementos: Peca[];
};

export type Travas = { arquetipo: boolean; cenario: boolean; elemento: boolean };

export type Opcoes = {
  subgenero: string | null;
  misturarMundos: boolean;
  incluirComuns: boolean;
};
```

- [ ] **Step 5: Rodar e ver passar**

Run: `npx vitest run src/lib/gerador/dados.test.ts`
Expected: PASS — 7 testes

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "Adiciona banco de complicações e moldes do gerador"
```

---

### Task 11: Gerador — sorteio das peças

Funções puras com o gerador de números aleatórios injetado, o que torna todo comportamento verificável sem depender de sorte.

**Files:**
- Create: `src/lib/gerador/sorteio.ts`, `src/lib/gerador/sorteio.test.ts`

**Interfaces:**
- Consumes: `tipos.ts`, `COMPLICACOES`.
- Produces:
  - `sortear(pools: Pools, opcoes: Opcoes, travas: Travas, anterior: Sorteio | null, aleatorio: () => number): Sorteio`
  - `poolsFiltrados(pools: Pools, opcoes: Opcoes): Pools`

- [ ] **Step 1: Escrever o teste**

`src/lib/gerador/sorteio.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { poolsFiltrados, sortear } from './sorteio';
import type { Opcoes, Pools, Sorteio, Travas } from './tipos';

const pools: Pools = {
  arquetipos: [
    { id: 'a1', nome: 'A Transumana', subgenero: 'cyberpunk' },
    { id: 'a2', nome: 'O Capitão Estratégico', subgenero: 'space-opera' },
    { id: 'a3', nome: 'A Mentora', subgenero: 'comuns' },
  ],
  cenarios: [
    { id: 'c1', nome: 'Megacidades', subgenero: 'cyberpunk', singular: 'uma megacidade' },
    { id: 'c2', nome: 'Ruínas Antigas', subgenero: 'space-opera', singular: 'uma ruína antiga' },
  ],
  elementos: [
    { id: 'e1', nome: 'Vigilância onipresente', subgenero: 'cyberpunk' },
    { id: 'e2', nome: 'Profecias cósmicas', subgenero: 'space-opera' },
  ],
};

const SEM_TRAVA: Travas = { arquetipo: false, cenario: false, elemento: false };
const base: Opcoes = { subgenero: 'cyberpunk', misturarMundos: false, incluirComuns: false };
const zero = () => 0;

describe('poolsFiltrados', () => {
  it('mantém apenas o subgênero escolhido', () => {
    const r = poolsFiltrados(pools, base);
    expect(r.arquetipos.map((a) => a.id)).toEqual(['a1']);
  });

  it('soma os arquétipos comuns quando pedido', () => {
    const r = poolsFiltrados(pools, { ...base, incluirComuns: true });
    expect(r.arquetipos.map((a) => a.id).sort()).toEqual(['a1', 'a3']);
  });

  it('deixa passar todos os mundos no modo misturar', () => {
    const r = poolsFiltrados(pools, { ...base, misturarMundos: true });
    expect(r.arquetipos).toHaveLength(3);
    expect(r.cenarios).toHaveLength(2);
  });
});

describe('sortear', () => {
  it('respeita o subgênero escolhido', () => {
    const s = sortear(pools, base, SEM_TRAVA, null, zero);
    expect(s.arquetipo.subgenero).toBe('cyberpunk');
    expect(s.cenario.subgenero).toBe('cyberpunk');
    expect(s.elemento.subgenero).toBe('cyberpunk');
  });

  it('preserva a peça travada', () => {
    const anterior = sortear(pools, { ...base, misturarMundos: true }, SEM_TRAVA, null, () => 0.9);
    const novo = sortear(
      pools, { ...base, misturarMundos: true },
      { arquetipo: true, cenario: false, elemento: false }, anterior, () => 0,
    );
    expect(novo.arquetipo).toEqual(anterior.arquetipo);
  });

  it('nunca repete a complicação da rolagem anterior', () => {
    let anterior: Sorteio | null = null;
    for (let i = 0; i < 50; i++) {
      const atual = sortear(pools, base, SEM_TRAVA, anterior, Math.random);
      if (anterior) expect(atual.complicacao).not.toBe(anterior.complicacao);
      anterior = atual;
    }
  });

  it('nunca repete a família da rolagem anterior', () => {
    let anterior: Sorteio | null = null;
    for (let i = 0; i < 50; i++) {
      const atual = sortear(pools, base, SEM_TRAVA, anterior, Math.random);
      if (anterior) expect(atual.familia).not.toBe(anterior.familia);
      anterior = atual;
    }
  });

  it('lança erro quando não há peça disponível', () => {
    const vazio: Pools = { arquetipos: [], cenarios: [], elementos: [] };
    expect(() => sortear(vazio, base, SEM_TRAVA, null, zero)).toThrow(/sem peças/i);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/gerador/sorteio.test.ts`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`src/lib/gerador/sorteio.ts`:

```ts
import { COMPLICACOES } from './complicacoes';
import type { Opcoes, Peca, PecaCenario, Pools, Sorteio, Travas } from './tipos';

function escolher<T>(lista: T[], aleatorio: () => number): T {
  if (lista.length === 0) throw new Error('sem peças disponíveis para sortear');
  return lista[Math.floor(aleatorio() * lista.length) % lista.length];
}

function pertence(peca: Peca, opcoes: Opcoes, aceitaComuns: boolean): boolean {
  if (opcoes.misturarMundos) return true;
  if (peca.subgenero === opcoes.subgenero) return true;
  return aceitaComuns && opcoes.incluirComuns && peca.subgenero === 'comuns';
}

export function poolsFiltrados(pools: Pools, opcoes: Opcoes): Pools {
  return {
    arquetipos: pools.arquetipos.filter((p) => pertence(p, opcoes, true)),
    cenarios: pools.cenarios.filter((p) => pertence(p, opcoes, false)) as PecaCenario[],
    elementos: pools.elementos.filter((p) => pertence(p, opcoes, false)),
  };
}

export function sortear(
  pools: Pools,
  opcoes: Opcoes,
  travas: Travas,
  anterior: Sorteio | null,
  aleatorio: () => number,
): Sorteio {
  const disponivel = poolsFiltrados(pools, opcoes);

  const familias = COMPLICACOES.filter((f) => f.nome !== anterior?.familia);
  const familia = escolher(familias, aleatorio);
  const complicacoes = familia.complicacoes.filter((c) => c !== anterior?.complicacao);
  const complicacao = escolher(complicacoes, aleatorio);

  return {
    arquetipo: travas.arquetipo && anterior ? anterior.arquetipo : escolher(disponivel.arquetipos, aleatorio),
    cenario: travas.cenario && anterior ? anterior.cenario : escolher(disponivel.cenarios, aleatorio),
    elemento: travas.elemento && anterior ? anterior.elemento : escolher(disponivel.elementos, aleatorio),
    complicacao,
    familia: familia.nome,
  };
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/gerador/sorteio.test.ts`
Expected: PASS — 8 testes (3 de `poolsFiltrados`, 5 de `sortear`)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "Adiciona sorteio de peças do gerador"
```

---

### Task 12: Gerador — redação da premissa

Onde moram as regras de português. Cada teste desta tarefa corresponde a um erro observado em teste com dados reais durante o design.

**Files:**
- Create: `src/lib/gerador/redacao.ts`, `src/lib/gerador/redacao.test.ts`

**Interfaces:**
- Consumes: `tipos.ts`, `MOLDES`.
- Produces:
  - `contrair(preposicao: 'em' | 'a', sintagma: string): string`
  - `generoDe(nome: string): 'f' | 'm'`
  - `emMinuscula(nome: string): string`
  - `redigir(sorteio: Sorteio, molde: string): string`

- [ ] **Step 1: Escrever o teste**

`src/lib/gerador/redacao.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { contrair, emMinuscula, generoDe, redigir } from './redacao';
import type { Sorteio } from './tipos';

const sorteio: Sorteio = {
  arquetipo: { id: 'a', nome: 'A Colecionadora de Artefatos', subgenero: 'space-opera' },
  cenario: { id: 'c', nome: 'Ruínas Antigas', subgenero: 'space-opera', singular: 'uma ruína antiga' },
  elemento: { id: 'e', nome: 'Busca por artefatos ancestrais', subgenero: 'space-opera' },
  complicacao: 'a cura existe, e é fabricada com a própria doença',
  familia: 'O preço do poder',
};

describe('contrair', () => {
  it('funde em + uma em numa', () => {
    expect(contrair('em', 'uma ruína antiga')).toBe('numa ruína antiga');
  });

  it('funde em + um em num', () => {
    expect(contrair('em', 'um abrigo subterrâneo')).toBe('num abrigo subterrâneo');
  });

  it('não funde a + uma', () => {
    expect(contrair('a', 'uma ruína antiga')).toBe('a uma ruína antiga');
  });
});

describe('generoDe', () => {
  it('deduz o gênero do artigo inicial', () => {
    expect(generoDe('A Transumana')).toBe('f');
    expect(generoDe('O Capitão Estratégico')).toBe('m');
  });
});

describe('emMinuscula', () => {
  it('abaixa só o artigo, preservando o nome', () => {
    expect(emMinuscula('A Colecionadora de Artefatos')).toBe('a Colecionadora de Artefatos');
    expect(emMinuscula('O Hacker')).toBe('o Hacker');
  });
});

describe('redigir', () => {
  it('monta a frase com contração e artigo em minúscula', () => {
    const frase = redigir(sorteio, '{em:cenario}, sob {elemento}, {arquetipo} descobre que {complicacao}.');
    expect(frase).toBe(
      'Numa ruína antiga, sob busca por artefatos ancestrais, ' +
        'a Colecionadora de Artefatos descobre que a cura existe, e é fabricada com a própria doença.',
    );
  });

  it('resolve o pronome pelo gênero do arquétipo', () => {
    const frase = redigir(sorteio, '{arquetipo} some {em:cenario} com {elemento} até {pronome} ver que {complicacao}.');
    expect(frase).toContain('até ela ver que');
  });

  it('usa ele quando o arquétipo é masculino', () => {
    const masculino = { ...sorteio, arquetipo: { id: 'b', nome: 'O Hacker', subgenero: 'cyberpunk' } };
    const frase = redigir(masculino, '{arquetipo} some {em:cenario} com {elemento} até {pronome} ver que {complicacao}.');
    expect(frase).toContain('até ele ver que');
  });

  it('capitaliza somente a primeira letra da frase', () => {
    const frase = redigir(sorteio, '{em:cenario}, sob {elemento}, {arquetipo} descobre que {complicacao}.');
    expect(frase[0]).toBe('N');
    expect(frase).toContain(', a Colecionadora');
  });

  it('não deixa marcador por resolver', () => {
    for (const molde of ['{em:cenario} {elemento} {arquetipo} {complicacao} {pronome} {a:cenario}.']) {
      expect(redigir(sorteio, molde)).not.toMatch(/\{|\}/);
    }
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run src/lib/gerador/redacao.test.ts`
Expected: FAIL — módulo não encontrado

- [ ] **Step 3: Implementar**

`src/lib/gerador/redacao.ts`:

```ts
import type { Sorteio } from './tipos';

const CONTRACOES: Record<string, Record<string, string>> = {
  em: { um: 'num', uma: 'numa', o: 'no', a: 'na', os: 'nos', as: 'nas' },
  a: {},
};

export function contrair(preposicao: 'em' | 'a', sintagma: string): string {
  const [artigo, ...resto] = sintagma.split(' ');
  const fundido = CONTRACOES[preposicao][artigo];
  return fundido ? `${fundido} ${resto.join(' ')}` : `${preposicao} ${sintagma}`;
}

export function generoDe(nome: string): 'f' | 'm' {
  return nome.startsWith('A ') ? 'f' : 'm';
}

export function emMinuscula(nome: string): string {
  return nome.charAt(0).toLowerCase() + nome.slice(1);
}

export function redigir(sorteio: Sorteio, molde: string): string {
  const { arquetipo, cenario, elemento, complicacao } = sorteio;

  const frase = molde
    .replaceAll('{em:cenario}', contrair('em', cenario.singular))
    .replaceAll('{a:cenario}', contrair('a', cenario.singular))
    .replaceAll('{cenario}', cenario.singular)
    .replaceAll('{elemento}', emMinuscula(elemento.nome))
    .replaceAll('{arquetipo}', emMinuscula(arquetipo.nome))
    .replaceAll('{pronome}', generoDe(arquetipo.nome) === 'f' ? 'ela' : 'ele')
    .replaceAll('{complicacao}', complicacao);

  return frase.charAt(0).toUpperCase() + frase.slice(1);
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run src/lib/gerador/redacao.test.ts`
Expected: PASS — 11 testes

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "Adiciona redação da premissa do gerador"
```

---

### Task 13: Gerador — interface

**Files:**
- Create: `src/pages/gerador.astro`
- Create: `src/lib/gerador/index.ts`

**Interfaces:**
- Consumes: `sortear`, `redigir`, `MOLDES`, coleções de conteúdo.

- [ ] **Step 1: Montar a estrutura da página**

A página lê as coleções no build e injeta os pools num `<script type="application/json">`, evitando qualquer requisição em tempo de execução.

`src/pages/gerador.astro`:

```astro
---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';

const subgeneros = await getCollection('subgeneros');
const mundos = subgeneros.filter((s) => s.data.mundo).sort((a, b) => a.data.ordem - b.data.ordem);

const pools = {
  arquetipos: (await getCollection('arquetipos')).map((a) => ({
    id: a.id, nome: a.data.nome, subgenero: a.data.subgenero,
  })),
  cenarios: (await getCollection('cenarios')).map((c) => ({
    id: c.id, nome: c.data.titulo, subgenero: c.data.subgenero, singular: c.data.singular,
  })),
  elementos: (await getCollection('elementos')).map((e) => ({
    id: e.id, nome: e.data.titulo, subgenero: e.data.subgenero,
  })),
};
---
<Base titulo="Gerador">
  <h1>Gerador de combinações</h1>

  <form id="opcoes">
    <label>Mundo
      <select name="subgenero">
        {mundos.map((m) => <option value={m.id}>{m.data.nome}</option>)}
      </select>
    </label>
    <label><input type="checkbox" name="misturarMundos" /> Misturar mundos</label>
    <label><input type="checkbox" name="incluirComuns" /> Incluir os 20 arquétipos comuns</label>
  </form>

  <section id="cartas" aria-live="polite">
    {['arquetipo', 'cenario', 'elemento'].map((tipo) => (
      <article class="cartao" data-carta={tipo}>
        <span class="etiqueta" data-papel="etiqueta"></span>
        <h2 data-papel="nome"></h2>
        <button type="button" class="botao" data-travar={tipo} aria-pressed="false">Travar</button>
      </article>
    ))}
  </section>

  <section aria-live="polite">
    <p id="premissa"></p>
    <button type="button" class="botao botao--principal" id="sortear">Sortear tudo</button>
    <button type="button" class="botao" id="reescrever">Reescrever</button>
    <button type="button" class="botao" id="copiar">Copiar</button>
    <span id="aviso" role="status"></span>
  </section>

  <script type="application/json" id="pools" set:html={JSON.stringify(pools)}></script>
</Base>
```

`aria-live="polite"` nas duas seções é o que faz um leitor de tela anunciar o resultado de cada rolagem; sem isso, quem não enxerga a tela clica em "sortear" e não recebe retorno nenhum.

- [ ] **Step 2: Escrever o script da página**

Acrescentar ao fim de `gerador.astro`:

```astro
<script>
  import { sortear } from '../lib/gerador/sorteio';
  import { redigir } from '../lib/gerador/redacao';
  import { MOLDES } from '../lib/gerador/moldes';
  import type { Opcoes, Pools, Sorteio, Travas } from '../lib/gerador/tipos';

  const pools: Pools = JSON.parse(document.getElementById('pools')!.textContent!);
  const form = document.getElementById('opcoes') as HTMLFormElement;
  const premissa = document.getElementById('premissa')!;
  const aviso = document.getElementById('aviso')!;

  const travas: Travas = { arquetipo: false, cenario: false, elemento: false };
  let atual: Sorteio | null = null;

  const lerOpcoes = (): Opcoes => {
    const dados = new FormData(form);
    return {
      subgenero: String(dados.get('subgenero')),
      misturarMundos: dados.get('misturarMundos') === 'on',
      incluirComuns: dados.get('incluirComuns') === 'on',
    };
  };

  const moldeAoAcaso = () => MOLDES[Math.floor(Math.random() * MOLDES.length)];

  function pintar(sorteio: Sorteio, molde: string) {
    for (const tipo of ['arquetipo', 'cenario', 'elemento'] as const) {
      const carta = document.querySelector(`[data-carta="${tipo}"]`)!;
      const peca = sorteio[tipo];
      carta.querySelector('[data-papel="nome"]')!.textContent = peca.nome;
      carta.querySelector('[data-papel="etiqueta"]')!.textContent = peca.subgenero.replace(/-/g, ' ');
    }
    premissa.textContent = redigir(sorteio, molde);
  }

  function rolar() {
    atual = sortear(pools, lerOpcoes(), travas, atual, Math.random);
    pintar(atual, moldeAoAcaso());
  }

  document.getElementById('sortear')!.addEventListener('click', rolar);

  // Reescrever mantém as peças e troca apenas molde e complicação, por isso
  // sorteia com as três travas ligadas.
  document.getElementById('reescrever')!.addEventListener('click', () => {
    if (!atual) return rolar();
    atual = sortear(pools, lerOpcoes(), { arquetipo: true, cenario: true, elemento: true }, atual, Math.random);
    pintar(atual, moldeAoAcaso());
  });

  document.getElementById('copiar')!.addEventListener('click', async () => {
    if (!premissa.textContent) return;
    await navigator.clipboard.writeText(premissa.textContent);
    aviso.textContent = 'Premissa copiada.';
    setTimeout(() => { aviso.textContent = ''; }, 2000);
  });

  for (const botao of document.querySelectorAll<HTMLButtonElement>('[data-travar]')) {
    botao.addEventListener('click', () => {
      const tipo = botao.dataset.travar as keyof Travas;
      travas[tipo] = !travas[tipo];
      botao.setAttribute('aria-pressed', String(travas[tipo]));
      botao.textContent = travas[tipo] ? 'Destravar' : 'Travar';
    });
  }

  form.addEventListener('change', () => { atual = null; rolar(); });
  rolar();
</script>
```

Trocar de mundo zera o sorteio anterior: sem isso, uma peça do mundo antigo poderia sobreviver por trava e contradizer a opção escolhida.

O sorteio anterior é o único estado guardado, e ele vive em memória — nada é gravado no navegador, conforme a decisão de gerador efêmero.

- [ ] **Step 3: Verificar à mão no navegador**

Run: `npm run dev` e abrir `/gerador`
Expected: sortear várias vezes seguidas produz frases variadas e gramaticalmente corretas; travar uma carta e re-rolar mantém aquela peça; "misturar mundos" traz peças de subgêneros diferentes; copiar coloca a frase na área de transferência

Rolar vinte vezes seguidas e ler todas as frases em voz alta. Qualquer uma que soe truncada ou com preposição errada indica molde a ajustar em `moldes.ts` — é o momento certo para descobrir isso, e é o que decide se o gerador vale a visita.

- [ ] **Step 4: Verificar por teclado**

Percorrer a página inteira usando só Tab, Espaço e Enter. Todo botão precisa ser alcançável e ter foco visível, e os botões de travar precisam anunciar o estado por `aria-pressed`.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "Adiciona interface do gerador"
```

---

### Task 14: Acessibilidade, contraste e verificação final

**Files:**
- Modify: `src/styles/global.css` conforme necessário
- Create: `docs/verificacao-visual.md`

- [ ] **Step 1: Medir contraste em cada mundo**

Para os seis mundos, medir a razão de contraste de três combinações sobre o painel: dourado, violeta e o corpo de texto. Registrar os valores em `docs/verificacao-visual.md`.
Expected: mínimo 4,5:1 para corpo de texto e 3:1 para os elementos grandes. Onde falhar, escurecer o painel aumentando a opacidade em vez de mudar os acentos, que são fixos por decisão de projeto.

- [ ] **Step 2: Conferir movimento reduzido**

Ativar "reduzir movimento" no sistema e recarregar.
Expected: a aurora fica parada em todas as páginas.

- [ ] **Step 3: Conferir em tela estreita**

Abrir a 360px de largura.
Expected: nenhuma rolagem horizontal; as cartas do gerador empilham.

- [ ] **Step 4: Ligar a verificação de tipos ao projeto**

Durante a Task 2 descobriu-se que o projeto não tem forma commitada de checar tipos: `tsc` só rodou por `npx` avulso, e `astro.config.ts` acusa `TS2591: Cannot find name 'process'` por falta de `@types/node`. Instalar `@types/node` como dependência de desenvolvimento e acrescentar ao `package.json` o script `"check": "astro check"`. Acrescentar `npm run check` ao fluxo de publicação, junto de `npx vitest run`.

Run: `npm run check`
Expected: nenhum erro de tipo, incluindo o `process` de `astro.config.ts`

- [ ] **Step 5: Rodar a suíte completa**

Run: `npx vitest run && npm run check && npm run build && cd scripts && python -m pytest migracao/tests/ -v`
Expected: tudo passa

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "Ajusta contraste e registra a verificação visual"
```

---

## Verificação do plano contra o documento de design

| Requisito do design | Onde é implementado |
|---|---|
| Conteúdo em Markdown, um arquivo por ideia | Task 2, Task 5 |
| Campos `felino`, `singular`, `mundo`, `aurora` | Task 2 (esquemas), Task 4 (singular), Task 5 (auroras) |
| Gênero deduzido do artigo do nome | Task 2 (validação), Task 12 (`generoDe`) |
| Rotas por tipo e por subgênero, com âncoras | Task 7 |
| Bloco "Como usar esta página" em cada seção | Task 7 |
| Rota `/mundos/` cruzando o outro eixo | Task 8 |
| Busca que ignora acentuação | Task 9 |
| Pôster neon sobre aurora, acentos fixos | Task 6 |
| Aurora variável por subgênero | Task 5 (cores), Task 6 (renderização) |
| Cartas com trava e re-rolagem | Task 11, Task 13 |
| Modo misturar mundos | Task 11 |
| Arquétipos comuns como opção desligada por padrão | Task 11, Task 13 |
| 40 complicações em 7 famílias | Task 10 |
| 10 moldes de frase | Task 10 |
| Contração de preposição, artigo em minúscula | Task 12 |
| Sem complicação nem família repetida em sequência | Task 11 |
| Resultado efêmero, só copiar | Task 13 |
| Migração roda uma vez, fica como registro | Tasks 3, 4, 5 |
| Publicação automática por GitHub Actions | Task 1 |
| Validação de conteúdo quebra o build | Task 2 |
| Licenças MIT e CC BY 4.0 | Task 1 |
| `prefers-reduced-motion` respeitado | Task 6, Task 14 |
| Contraste verificado por mundo | Task 14 |

Fora do escopo, conforme a seção 9 do design: blog, link compartilhável, caderno local, card de imagem, filtros por papel narrativo, afinidade temática, tema claro e outros idiomas.
